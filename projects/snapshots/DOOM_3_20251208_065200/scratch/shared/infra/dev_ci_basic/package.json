{
  "name": "dev-ci-basic",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173",
    "ci-build": "npm ci --silent || npm install --silent && npm run build"
  },
  "devDependencies": {
    "vite": "^4.4.0",
    "typescript": "^5.4.4"
  }
}
