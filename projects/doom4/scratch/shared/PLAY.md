# Playbook

Install and run:
- npm install
- npm run dev

Development notes:
- Frontend scaffolds communicate with backend at /api/* endpoints
- World persistence via /api/world/save and /api/world/load
