# Development runtime image for ECS dev workspace
FROM node:18-bullseye-slim
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm ci --silent
COPY . .
EXPOSE 3000
CMD ["bash", "scripts/ci-run.sh"]

# End of file note: this Dockerfile.dev provides a quick dev runtime for CI stub execution
