name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies (auth)
        working-directory: scratch/shared/services/auth_service
        run: |
          python -m pip install --upgrade pip
          pip install fastapi uvicorn python-jose pydantic
      - name: Run unit tests (auth placeholder)
        working-directory: scratch/shared/services/auth_service
        run: |
          echo 'No tests yet; placeholder step'
