# Levels Admin UI (vanilla) - summary

This folder contains a lightweight static UI and a vanilla JS implementation to interact with the /levels API exposed by the backend. It demonstrates list, detail, and modal create/edit flows with loading and error states.
