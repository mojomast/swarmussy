# Engine core skeleton (high level)

This is a minimal engine core to integrate with the in-memory world used in tests. It exposes Engine.start, Engine.tick, Engine.stop, Engine.status, and helpers to inspect/get world state.
