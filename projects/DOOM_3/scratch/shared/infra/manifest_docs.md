# Infra deployment manifests and docs

- Kubernetes: cleanup deployment.yaml (updated to point to repo-backed API container)
- Docker Compose: cleanup/compose.yml and app/docker-compose.yml updated for repository-backed API
- Start script: app/start_api.sh supports REPO_API_URL, REPO_API_BRANCH
