name: CI
on:
  push:
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Build and push image
        run: |
          docker build -t cleanup/api:latest ./app
          echo "Assuming registry is pre-authenticated in this CI environment."
